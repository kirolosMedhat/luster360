import 'package:flutter/material.dart';

class LusterColors {
  LusterColors._();

  // Primary Brand Identity
  static const Color primaryBlue = Color(0xFF86CFFF);
  static const Color primary = primaryBlue;
  static const Color darkNavy = Color(0xFF18283F);
  static const Color pitchBlack = Color(0xFF000000);
  static const Color pureWhite = Color(0xFFFFFFFF);

  // Background and Surfaces (Dark Booth Studio Environment)
  static const Color background = Color(0xFF0B0F17);
  static const Color canvas = background;
  static const Color panel = Color(0xFF101622);
  static const Color surface = Color(0xFF151D2E);
  static const Color header = Color(0xFF18283F);

  // Borders and Dividers
  static const Color border = Color(0xFF1E293B);
  static const Color borderLight = Color(0xFF2A3B53);

  // Typography
  static const Color text = Color(0xFFF8FAFC);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textDark = Color(0xFF64748B);

  // Functional & Semantic States
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFEF4444);
  static const Color info = Color(0xFF38BDF8);

  // Booth Operational States
  static const Color stateRecording = Color(0xFFEF4444);
  static const Color stateRendering = Color(0xFFF59E0B);
  static const Color stateUploading = Color(0xFF86CFFF);
  static const Color stateReady = Color(0xFF10B981);
  static const Color stateOffline = Color(0xFF64748B);

  // Glassmorphism and Overlays
  static const Color overlayBackdrop = Color(0xCC0B0F17);
  static const Color surfaceGlass = Color(0x99151D2E);
  static const Color accentMuted = Color(0x2686CFFF);
}
